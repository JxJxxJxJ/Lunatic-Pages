// not /lua y todo lo que tiene adentro
!/lua

// not luasnippets y todo lo que tiene adentro
!/luasnippets

// not config.lua stylua.toml solo del directorio root
!/config.lua
!/stylua.toml

// Everything else
*
